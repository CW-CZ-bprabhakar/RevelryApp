# Uncomment this line to define a global platform for your project
platform :ios, '9.0'

target 'RevelryTest' do

  use_frameworks!
  pod 'Alamofire', '~> 3.4'
  pod 'MBProgressHUD', '~> 1.0.0'
  # Pods for RevelryTest

  target 'RevelryTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
